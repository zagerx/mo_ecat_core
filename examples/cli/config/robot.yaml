# CLI 人形机器人配置模板。
# EtherCAT 核心库不解析本文件；由应用层代码生成器在构建期转换为
# struct robot_config。

version: "1.0"
robot:
  name: "humanoid"
  joints:
    - joint_name: "left_shoulder_pitch"
      group: "left_arm"
      identity:
        position: 1
        vendor_id: 0x00000766
        product_code: 0x00002001
